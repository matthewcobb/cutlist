# Optional: override the config file path (default: ../cutlist.config.yaml)
# CUTLIST_CONFIG_PATH=/path/to/cutlist.config.yaml

# PostgreSQL connection string (required for project persistence)
DATABASE_URL=postgres://cutlist:cutlist@localhost:5432/cutlist
