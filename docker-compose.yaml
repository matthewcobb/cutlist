services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: cutlist
      POSTGRES_PASSWORD: cutlist
      POSTGRES_DB: cutlist
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
